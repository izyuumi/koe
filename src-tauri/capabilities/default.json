{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Koe window",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "shell:default"
  ]
}
